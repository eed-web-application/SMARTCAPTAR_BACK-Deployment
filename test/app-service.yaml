apiVersion: v1
kind: Service
metadata:
  name: smartcaptar-back-service
spec:
  selector:
    app: smartcaptar-back
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
