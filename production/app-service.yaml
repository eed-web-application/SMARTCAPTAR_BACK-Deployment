apiVersion: v1
kind: Service
metadata:
  name: smartcaptar-back-service
spec:
  selector:
    app: smartcaptar-back
  ports:
    - name: http
      protocol: TCP
      port: 5000
      targetPort: 1337
